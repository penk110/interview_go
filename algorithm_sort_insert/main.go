package main

import (
	"log"
)

// InsertSort is sort method by compare sort_insert value
func InsertSort(target []int) {
	l := len(target)
	for i := 1; 1 <= l-1; l++ {
		deal := target[i]
		j := i + 1
		if deal < target[j] {

		}

	}
}

func main() {
	log.Println("logging ...")

}
